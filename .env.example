# Supabase Configuration
# Copy this file to .env.local and fill in your actual values
# .env.local is in .gitignore and will NOT be committed to GitHub

# Your Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Your Supabase Anon/Public Key (safe for client-side)
# Get this from: Supabase Dashboard > Settings > API > Project API keys > anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Your Supabase Service Role Key (SECRET - only for server-side operations)
# Get this from: Supabase Dashboard > Settings > API > Project API keys > service_role secret
# ⚠️ NEVER expose this to the client-side code
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Knot API (when available)
NEXT_PUBLIC_KNOT_API_KEY=
KNOT_API_SECRET=
KNOT_WEBHOOK_SECRET=

# App URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
